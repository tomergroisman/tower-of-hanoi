upstream server {
  server server:3232;
}

upstream client {
  server client:3000;
}

server {
  listen 80;

  location /server {
    proxy_pass http://server;
  }

  location / {
    proxy_pass http://client;
  }
}