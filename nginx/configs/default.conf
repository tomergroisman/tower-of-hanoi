upstream server {
  server server:3232;
}

upstream client {
  server client:3000;
}

server {
  listen 80;

  location / {
    proxy_pass http://client;
  }
}

server {
  listen 80;
  server_name server.*;

  location / {
    proxy_pass http://server;
  }
}